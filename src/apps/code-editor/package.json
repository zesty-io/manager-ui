{
  "name": "code-editor",
  "version": "1.0.0",
  "description": "Application for managing code on an instance",
  "main": "src/index.js",
  "scripts": {
    "test": "cross-env ../../../node_modules/.bin/cypress run",
    "test:open": "cross-env ../../../node_modules/.bin/cypress open",
    "start": "cross-env ../../../node_modules/.bin/webpack --display=minimal --progress --watch",
    "build": "cross-env ../../../node_modules/.bin/webpack --display=errors-only --progress -p"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
