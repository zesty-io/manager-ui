{
  "name": "leads",
  "version": "1.0.0",
  "description": "Application for managing forms submitted on an instance",
  "main": "./src/index.js",
  "scripts": {
    "test": "cypress run",
    "start": "cross-env ../../../node_modules/.bin/webpack --display=minimal --progress --watch",
    "build": "cross-env ../../../node_modules/.bin/webpack --display=errors-only --progress -p"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
