import { FilterButton } from "./FilterButton";

// TODO: Add functionality
export const People = () => {
  return (
    <FilterButton
      isFilterActive={false}
      buttonText="People"
      onOpenMenu={() => {}}
      onRemoveFilter={() => {}}
    />
  );
};
