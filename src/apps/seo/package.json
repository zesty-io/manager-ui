{
  "name": "seo",
  "version": "1.0.0",
  "description": "Application for managinge Search Engine Optimizations of an instance",
  "main": "./src/index.js",
  "scripts": {
    "test": "cypress run",
    "start": "cross-env ../../../node_modules/.bin/webpack --display=minimal --progress --watch",
    "build": "cross-env ../../../node_modules/.bin/webpack --display=errors-only --progress -p"
  },
  "devDependencies": {
    "cross-env": "^7.0.2"
  }
}
